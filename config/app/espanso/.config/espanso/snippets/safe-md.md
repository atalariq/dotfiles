Output this as copy-safe Markdown.

Rules:
- Do not wrap the entire response in a backtick code fence.
- If a full Markdown block is required, use tilde fences: ~~~md.
- Use backtick fences only for inner code/prompt examples.
- Avoid nested backtick fences.
- For long documents, split into sections instead of one huge block.
